{
  "name": "@ai-playground/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsup src/server.ts --format esm --dts false --out-dir dist",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ai-playground/shared": "workspace:*",
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^8.0.0",
    "@google/generative-ai": "^0.11.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.27.2",
    "openai": "^4.55.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsup": "^8.0.1",
    "typescript": "^5.4.5"
  }
}
